# Placeholder Assets

このディレクトリは仮素材置き場です。

`assets/manifest.yaml` の active entry が `kind: placeholder` または `path: res://assets/placeholder/...` を指す場合、ゲーム右上に `PLACEHOLDER ASSET` が表示されます。

release/check では placeholder entry が残っていると失敗します。
